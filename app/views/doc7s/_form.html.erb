<%= form_for([@doc7.tender, @doc7]) do |f| %>
  <% if @doc7.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@doc7.errors.count, "error") %> prohibited this doc7 from being saved:</h2>

      <ul>
      <% @doc7.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
