<p id="notice"><%= notice %></p>

<p>
  <strong>Title:</strong>
  <%= @tender.title %>
</p>

<p>
  <strong>Body:</strong>
  <%= @tender.body %>
</p>

<% if can? :update , @tender %>
<%= link_to 'Edit', edit_tender_path(@tender) %> |
<% end %>
<%= link_to 'Back', tenders_path %>
<br>

<% if @tender.user_id == current_user.id  %>
<%= link_to 'Doc1', tender_doc1s_path(@tender.id) %>
<%= link_to 'Doc5', tender_doc5s_path(@tender.id) %>
<%= link_to 'Doc6', tender_doc6s_path(@tender.id) %>
<%= link_to 'Doc7', tender_doc7s_path(@tender.id) %>
<% end %>